#include<stdio.h>

int main()
{
    int A[20], N, i, j, t;

    printf("Enter the length of Array : ");
    scanf("%d", &N);

    printf("Enter the array elements : ");

    for ( i = 0; i < N; i++)
    {
        for ( j = 0; j < N; j++)
        {
                
        }
        
    }
    
}